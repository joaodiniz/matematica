var rad, area;
rad = prompt("Raio")
while (rad != null) {
  area = 3.14 * rad * rad
    alert("radius = " + rad + ", area = " + area)
    document.write("<br> radius = " + rad + ", area = " + area)
  rad = prompt("Raio")
}
